> **📄 요약 ·** 세션 핸드오프(재개 진입점). 두 실험 상태·데이터 규칙·검증 수치·다음 할 일·핵심 쿼리. 새 세션은 이 문서부터.

# 🧠 작업 핸드오프 / RESUME — 2026-06-22

> 이 문서 하나만 읽으면 오늘까지의 맥락을 복원하고 바로 이어서 작업할 수 있도록 작성됨(자기완결형).
> 대상: 미래의 나 / 다른 분석가 / 새 AI 세션.

---

## 0. 다시 시작할 때 (3스텝)
1. **이 문서 + [`INDEX.md`](../INDEX.md)** 를 먼저 읽는다 (폴더 지도).
2. 데이터 작업이면 **§5 API 접근** + **§3 데이터 규칙**을 확인하고, **§8 핵심 쿼리**를 복붙해 현황 재확인.
3. **§6 TODO** 에서 다음 할 일을 고른다. (가장 급한 건 6/28 전 사전 스냅샷 박제)

---

## 1. 프로젝트 한 줄 정의
가짜연구소 성장시스템에서 **"중간 회고 가이드"가 완주율을 높이는가**를 검증하는 준실험. 데이터는 **PseudoLab 데이터 플랫폼(API)** 에서 SQL로 분석한다.

**폴더 지도** (2026-06-22 하이브리드 재배치 — 최신은 `INDEX.md`)
```
00_common/        공통: playbook·platform·scripts·data-structure-map·discussion
01_experiments/
  01_reflection/  1차 회고: experiment-spec·analysis-plan·data-definition
                  queries·report·announcement
  02_sidebar/     2차 사이드바: experiment-spec·data-definition
                  implementation-spec·queries
ideas/          미착수 아이디어
03_memory/      ← 본 핸드오프
```
> 원칙: 공통=`00_common/`, 실험전용=`01_experiments/<실험>/`. 예전 `00~04`·`query.md`·`03_report`·`04_script` 경로는 폐기됨.

---

## 2. 오늘(6/22) 한 일
1. 흩어진 문서 정리 → `00~04` 레이어 구조화, 중복 `github.md` 삭제, `INDEX.md` 생성.
2. 회고 실험 **분석계획서**(`02_.../analysis-plan.md`) 작성 — 추정량·공변량·SQL 골격·편향 통제.
3. PseudoLab API 연결, **적재 방식(snapshot/incremental) 검증** — snapshot 중복·`dm_*` 마트 `as_of_date` 발견.
4. **청강생(auditor) 오염 발견·보정** — `query.md` 전면 재작성(`dl_*`→`dm_*` 마트, `participant_type='regular'`), `analysis-plan.md` 모집단 정의 갱신.
5. **실제 수치 산출** + 방법론 준수 점검(§4).
6. **참여자용 리포트**(`03_report/`) + **데이터구조 맵**(md+drawio+png+svg) 작성.
7. **Discord 공지 스크립트**(`04_script/`) 작성.
8. 데이터셋 댓글 2건 등록(§7).

---

## 3. 반드시 기억할 데이터 규칙 ⚠️
1. **청강생 제외**: 출석/리텐션/이탈/완주는 `dl_project_members`로 하면 안 됨(`role`에 `runner/builder/member`만, **auditor 없음**). → **`dm_member_weekly_attendance` + `participant_type='regular'` + `member_status='active'`** 사용.
2. **스냅샷 날짜 필터 필수**: snapshot 테이블은 `base_date=(SELECT MAX(base_date) ...)`, **`dm_*` 마트는 `as_of_date=(SELECT MAX(as_of_date) ...)`**. 안 하면 30~53배 중복.
3. **쿼리 엔진 제약**: `SELECT`만, `LIMIT ≤ 100`, **세미콜론 불가**, **CTE(`WITH`) 미지원 → FROM 서브쿼리로**, 5초 타임아웃.
4. **manifest row_count는 근사치** — 정확 수치는 쿼리로(예: dl_projects manifest 23,652 vs 라이브 8,760).
5. **모집단 먼저 고정**: "6/28 시점 active 정규 멤버"(이탈자·청강생 제외) → 생존자/선택 편향 차단.

---

## 4. 검증된 실제 수치 (재실행 불필요 · 최신 스냅샷 2026-06-20)
**범위**: 12기 정규 멤버 **195명**, **28~30개 프로젝트(전부 active, 완료 0)**, 시즌 **14주차/16주**

| 지표 | 값 |
|---|---|
| 전체 출석률(정규) | **66.5%** |
| 4주차 리텐션 | **82.8%** (W1 169명 → W4 140명) |
| 이탈률 | **2.4%** (W1 169명 중 4명) |
| 완주율(Primary) | **측정 불가**(시즌 미종료) |
| 완주 진행 프록시 | 평균 마지막 출석 9.5주차, 8주+ 147명(75%)·12주+ 55명(28%) |
| 청강생 오염 | 청강생 32명 **74.1%** (정규보다 높아 섞으면 상향 편향) |
| snapshot 중복 | dl_projects 30일×292=8,760(30배) · dm_member 133,919 vs grain 2,526(~53배) |
| 기수 커버리지 | 출석 마트는 사실상 **12기만**(11·10기 각 1명) → new/returning 분리 불가 |

**방법론 준수 요약**: 데이터 위생(snapshot·청강생·생존자편향)·해석원칙·Guardrail-first ✅. 미충족 3가지 → ⚠ Primary KPI 미측정(시즌 미종료), ⛔ 프로젝트 단위 표본 부족(n≥30 단 1개), ⛔ new/returning 분리 불가(데이터 한계).

---

## 5. 데이터 접근 (API)
- **Base URL**: `https://<pseudolab-api-base-url>` (마스킹 — 커뮤니티 내부 공유)
- **인증 헤더**: `Authorization: Bearer <PAT>`  (PAT = Personal API Token)
- **토큰**: 보안상 여기에 평문 저장 안 함. PAT는 `plat_****`(전체 마스킹). 전체 토큰은 커뮤니티 데이터 대시보드(URL 마스킹)에서 발급/재확인.
- **가이드**: 데이터 `<대시보드>/llms.txt`(URL 마스킹) · discussion 작성은 `discussion.md`
- **주요 엔드포인트**
  - `GET /api/ai/manifest` — 테이블 목록
  - `POST /api/query` body `{"sql":"..."}` — 조회(제약: §3-3)
  - `GET/POST /api/datasets/{dataset_id}/comments` — 데이터셋 댓글(작성 시 `source:"ai-assisted"`)
  - discussion: `GET/POST /api/discussions...` (상세 `discussion.md`)

---

## 6. 다음에 할 일 (TODO · 우선순위)
> 🔴 6/28 전 · 🟡 7/5 전후 · 🟢 시즌 종료 후

- [ ] 🔴 **회고 이벤트/테이블 적재 확인** — `event_log`(`project_reflection_ui_viewed/clicked`), `reflection`(`experiment_id='s12-mid-reflection'`). *현재 매니페스트에 없음.*
- [ ] 🔴 **6/28 사전 스냅샷 박제** — 출석·발표·산출물·activity_score (공변량). *지나가면 복원 불가.*
- [ ] 🔴 **전원 노출 보장** — 알림 + 스터디 시작 홍보(공지 `01_experiments/01_reflection/announcement.md`).
- [ ] 🟡 **완주(完走) 정의 확정** — `session_submissions`(빈 테이블)·`dl_project_tasks`(status=done)·last_attended_week 프록시 중 택.
- [ ] 🟡 **new/returning 대체 소스** — `dl_project_members`(전 기수) 또는 `dl_project_applications`(season).
- [ ] 🟡 **표본 크기 게이트** — 작성/미작성 분할 후 그룹당 n≥30 확인. 프로젝트 단위는 탐색만.
- [ ] 🟢 **7/5 이후 1차 분석** — 퍼널(viewed→clicked→submitted) + 작성자 vs 미작성자(SMD 균형검사→Bayesian).
- [ ] 🟢 **완주율 최종 판정** — ship/hold/rollback/need_more_data (Guardrail 확인 후).

---

## 7. 알아둘 함정 / 기록
- 등록한 데이터셋 댓글 2건(`source=ai-assisted`):
  - `pseudolab.project-members.v1` [limitation] — role에 auditor 없음 → 청강생 오염, dm 마트 사용.
  - `pseudolab.dm-member-weekly-attendance.v1` [limitation] — 청강생 제외법 + 마트 12기만 커버.
  - (dm의 `as_of_date` 필터 팁은 기존 댓글에 이미 있어 작성 안 함.)
- PII 테이블 주의: `profiles, project_applications, registration_submissions, dm_member_weekly_attendance, dm_user_daily_activity, *_submissions`.
- 빈 테이블(row_count 0) 다수 — 분석 전 존재 확인(`session_submissions` 등).

---

## 8. 핵심 쿼리 (복붙용 · 검증 완료)
> 전체는 각 실험의 `queries.md`(예: `01_experiments/01_reflection/queries.md`). 공통 필터: `as_of_date=MAX` + `participant_type='regular'` + `member_status='active'` + `cohort='12'`.

**전체 출석 + 4주 리텐션**
```sql
SELECT COUNT(DISTINCT user_id) AS regular_users,
  ROUND(SUM(attended_count)*100.0/NULLIF(SUM(session_count),0),1) AS attendance_rate,
  COUNT(DISTINCT CASE WHEN week_number=1 AND attended=1 THEN user_id END) AS w1,
  COUNT(DISTINCT CASE WHEN week_number=4 AND attended=1 THEN user_id END) AS w4
FROM dm_member_weekly_attendance
WHERE as_of_date=(SELECT MAX(as_of_date) FROM dm_member_weekly_attendance)
  AND cohort='12' AND participant_type='regular' AND member_status='active'
```

**이탈율 (CTE 불가 → FROM 서브쿼리)**
```sql
SELECT project_title,
  COUNT(CASE WHEN w1=1 THEN 1 END) AS w1_count,
  COUNT(CASE WHEN w1=1 AND w2plus=0 THEN 1 END) AS dropout_count
FROM (SELECT user_id, project_title,
        MAX(CASE WHEN week_number=1 AND attended=1 THEN 1 ELSE 0 END) AS w1,
        MAX(CASE WHEN week_number>=2 AND attended=1 THEN 1 ELSE 0 END) AS w2plus
      FROM dm_member_weekly_attendance
      WHERE as_of_date=(SELECT MAX(as_of_date) FROM dm_member_weekly_attendance)
        AND cohort='12' AND participant_type='regular' AND member_status='active'
      GROUP BY user_id, project_title) t
GROUP BY project_title ORDER BY dropout_count DESC LIMIT 100
```

**청강생 오염 점검**
```sql
SELECT participant_type, COUNT(DISTINCT user_id) AS users,
  ROUND(SUM(attended_count)*100.0/NULLIF(SUM(session_count),0),1) AS attendance_rate
FROM dm_member_weekly_attendance
WHERE as_of_date=(SELECT MAX(as_of_date) FROM dm_member_weekly_attendance) AND cohort='12'
GROUP BY participant_type
```

---

## 9. 파일 인덱스 (핵심)
| 무엇 | 경로 |
|---|---|
| 폴더 지도 | `INDEX.md` |
| 방법론 | `00_common/playbook.md` |
| 플랫폼 아키텍처 | `00_common/platform.md` |
| 데이터구조 맵 | `00_common/data-structure-map.{md,drawio,png,svg}` |
| 통계 스크립트 | `00_common/scripts/` (calc_sample_size·srm_check·bayesian_calc·assign_variant) |
| 1차 회고 | `01_experiments/01_reflection/` (experiment-spec·analysis-plan·data-definition·queries·report·announcement) |
| 2차 사이드바 | `01_experiments/02_sidebar/` (experiment-spec·data-definition·implementation-spec·queries) |

---

## 10. 사이드바 A/B 실험 (sidebar-nav-v1) — 신규(6/22 설계 완료)

**정의**: pseudo-lab.com 웹 사이드바에서 파일 확장자 제거 + Projects/Events 최상단 → 핵심메뉴 CTR·등록 전환율 ↑ 검증. **회고와 달리 진짜 무작위 A/B**(50:50, 개인 단위) → 인과 해석 가능.

**태스크 15개 상태** (TaskList)
- ✅ 완료 8: #1 변형 · #2 KPI · #3 배정 · #4 표본 · #5 이벤트 · #6 노출 · #11 등록서 · #13 쿼리
- 📐 개발 핸드오프 5(명세+레퍼런스 완료, 플랫폼 코드 남음): #7 event_log 파이프라인 · #8 배정서비스 · #9 프론트 · #10 SRM모니터 · #12 대시보드
- ⏳ 대기 2: #14 배포(6/28~) · #15 판정(데이터 후)

> **이 7개(=5+2)가 "남은 것"인 이유**: 이 폴더는 기획 워크스페이스라 실제 플랫폼 코드 빌드·배포·결과판정은 여기서 실행 불가. 명세까지만 끝내고 정직하게 미완으로 둠.

**검증된 실수치 (scripts/ 실행)**
- 표본(#4): MAU 500(그룹당 250)엔 필요 n 377~3,397 → **탐색 실험으로 분류**. 결정실험화엔 MDE +8%p & 누적 노출 ≥450/그룹 필요.
- 배정(#3): 50.02%·sticky=True · SRM 정상/차단 동작 · Bayesian 예시 P(T>C)=96.4%.

**다음 분기점**: 운영팀과 (a) MDE 기대치 크게 / (b) 운영기간 길게 누적 / (c) 탐색 실험 합의 — 중 결정. 이후 #7 event_log(회고와 공용)부터 개발 착수.

**계측 핵심**: `event_log`는 회고(`project_reflection_ui_*`)와 **공용 테이블**. 이벤트: `exp_exposure`(분모)·`sidebar_item_clicked`·`page_view`·`enrollment_completed`·`project_alert_signup`. 노출=분모, 미노출 제외.
